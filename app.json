{
	"name": "Intercom Reverse Proxy",
	"description": "Simple Intercom reverse proxy for Heroku",
	"keywords": [
		"reverse proxy",
		"nginx"
	],
	"repository": "https://github.com/kfrancis/heroku-reverse-proxy-1",
	"buildpacks": [
		{
			"url": "https://github.com/heroku/heroku-buildpack-nginx"
		}
	],
	"env": {
		"UPSTREAM_SERVER": {
			"description": "URL of upstream server to which all requests should be proxied"
		},
		"CUSTOM_DOMAIN": {
			"description": "The URL the clients connect to"
		}
	},
	"stack": "heroku-16"
}
